# ── 絕對不能進版控的東西 ─────────────────────────────────────────────────────
# 每一條都寫明理由。原始系統的教訓：只寫 pattern 不寫理由，下一個人（包括三個月
# 後的自己）就會把它當成可有可無的整理，然後在某次「清理 gitignore」時刪掉。

# 群組對話的落地資料。這是整個 repo 最敏感的東西——裡面是群組成員的逐字發言，
# 而群組裡往往坐著公司以外的人（廠商、客戶）。
# 開頭的斜線是刻意的：只擋 repo 根目錄那個 data/，不要連
# review/static/data/ 底下的合成樣本一起擋掉（那個該進版控）。
/data/
*.db
*.db-wal
*.db-shm

# 群組對應表：含真實的 LINE group ID。group ID 本身不是機密，但它是「哪個群組
# 正在被錄」的清單，而且一旦公開就等於指認了對象。只有 .example 版本進版控。
groups.csv

# 金鑰
.env

# 擷取下來的媒體（照片、PDF）——同 data/，是別人傳進群組的東西
media/
line_capture_media/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
